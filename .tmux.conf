#Uncomment these only for the single parent tmux session (if nesting tmux sessions)
#unbind-key C-b 
#set-option -g prefix C-f

#set-option -g default-command "reattach-to-user-namespace -1 bash"
#bind-key f send-prefix

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'git@github.com/user/plugin'
# set -g @plugin 'git@bitbucket.com/user/plugin'
set -g @plugin 'tmux-plugins/tmux-resurrect'

set -g @plugin 'tmux-plugins/tmux-yank'

set -g @shell_mode 'vi'
unbind-key -t vi-copy v
setw -g mode-keys vi
bind-key -t vi-copy 'v' begin-selection # Begin selection in copy mode.
bind-key -t vi-copy 'C-v' rectangle-toggle # Begin selection in copy mode.
bind-key -t vi-copy 'y' copy-selection # Yank selection in copy mode.

# Copy SAM results directory to buffer
bind-key 'S' copy-mode \; send-keys C-b [ ? results/SAM_ Enter E Space B Enter

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
